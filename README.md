# AR Shooter Game Backend

Система распознавания лиц для AR-игры шутера с временными цифровыми профилями игроков.

## Архитектура

Система состоит из трех основных компонентов:

1. **Go Backend** (`shoter-backend`) - основной API сервер, игровая логика
2. **Python Face Recognition Service** (`shoter-face_ifentifier`) - сервис распознавания лиц
3. **PostgreSQL** - база данных для хранения профилей игроков
4. **Android Client** (`shoter-android_client`) - мобильное приложение

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Git

### Запуск всей системы

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd shoter-project
```

2. Запустите все сервисы:
```bash
docker-compose up --build
```

3. Проверьте что все сервисы запущены:
```bash
# Проверка Go backend
curl http://localhost:8080/api/game/status?gameId=test

# Проверка Python face service
curl http://localhost:5001/health
```

## API Документация

### Создание профиля игрока

**POST** `/api/create-player-profile`

```json
{
  "playerId": "player-123",
  "images": [
    "base64_encoded_front_image",
    "base64_encoded_left_image", 
    "base64_encoded_right_image",
    "base64_encoded_back_image"
  ],
  "angles": ["front", "left", "right", "back"]
}
```

**Ответ:**
```json
{
  "success": true,
  "playerId": "player-123",
  "message": "Player profile created successfully"
}
```

### Начало игры

**POST** `/api/game/start`

```json
{
  "gameMode": "pvp",
  "maxPlayers": 4,
  "gameDuration": 15,
  "playerIds": ["player-123", "player-456"]
}
```

**Ответ:**
```json
{
  "success": true,
  "gameId": "game-1234567890-5678",
  "sessionStatus": "started",
  "activePlayers": 2
}
```

### Выстрел

**POST** `/api/game/shot`

```json
{
  "shooterId": "player-123",
  "image": "base64_encoded_camera_image",
  "timestamp": 1687891234567
}
```

**Ответ:**
```json
{
  "success": true,
  "targetId": "player-456",
  "hitLocation": "body",
  "damage": 50,
  "targetHealth": 50,
  "isEliminated": false
}
```

### Статус игры

**GET** `/api/game/status?gameId=game-1234567890-5678`

**Ответ:**
```json
{
  "gameId": "game-1234567890-5678",
  "status": "active",
  "players": {
    "player-123": {
      "health": 100,
      "hits": 1,
      "eliminations": 0,
      "isActive": true
    },
    "player-456": {
      "health": 50,
      "hits": 0,
      "eliminations": 0,
      "isActive": true
    }
  },
  "duration": 15
}
```

### Работа с сохраненными профилями в БД

#### Получение профиля из БД

**GET** `/api/player-profile/{playerId}`

**Ответ:**
```json
{
  "success": true,
  "playerId": "player-123",
  "embeddings": {
    "front": [0.1, 0.2, ...],
    "left": [0.3, 0.4, ...],
    "right": [0.5, 0.6, ...],
    "back": [0.7, 0.8, ...]
  }
}
```

#### Удаление профиля из БД

**DELETE** `/api/player-profile/{playerId}`

**Ответ:**
```json
{
  "success": true,
  "message": "Profile deleted successfully"
}
```

#### Загрузка профиля из БД в face service

**POST** `/api/load-player-profile/{playerId}`

**Ответ:**
```json
{
  "success": true,
  "message": "Profile loaded to face service successfully"
}
```

## Тестирование

### Быстрое тестирование

Запустите простой тест системы:

```bash
./simple_test.sh
```

### Юнит-тесты

```bash
# Запуск всех юнит-тестов
cd shoter-backend
make test

# Запуск тестов с покрытием
make test-coverage

# Генерация HTML-отчета о покрытии
make test-coverage-html

# Проверка минимального покрытия (90%)
./check_coverage.sh
```

### Интеграционные тесты

Интеграционные тесты проверяют взаимодействие с реальными внешними системами:

```bash
# Запуск всех интеграционных тестов
make test-integration

# Запуск только тестов базы данных
make test-db

# Запуск только тестов Face Service
make test-face

# Запуск всех тестов (юнит + интеграционные)
make test-all
```

### Тестирование в Docker

```bash
# Запуск интеграционных тестов в Docker с тестовой БД и Face Service
make test-integration-docker

# Очистка тестовых контейнеров
make test-integration-docker-clean
```

### Настройка интеграционных тестов

Для запуска интеграционных тестов установите переменные окружения:

```bash
# База данных
export TEST_DB_HOST=localhost
export TEST_DB_PORT=5432
export TEST_DB_USER=postgres
export TEST_DB_PASSWORD=password
export TEST_DB_NAME=shoter_test

# Face Service
export TEST_FACE_SERVICE_URL=http://localhost:5000

# Включить интеграционные тесты
export RUN_INTEGRATION_TESTS=true
```

### Покрытие тестами

Проект имеет более 90% покрытия тестами, включая:
- ✅ Юнит-тесты для всех компонентов
- ✅ Интеграционные тесты с базой данных PostgreSQL
- ✅ Интеграционные тесты с Face Service
- ✅ End-to-end тесты полных сценариев

Это обеспечивает высокое качество кода и надежность системы.

### Тестирование создания профиля

```bash
# Создаем тестовый профиль (требуются реальные base64 изображения с лицами)
curl -X POST http://localhost:8080/api/create-player-profile \
  -H "Content-Type: application/json" \
  -d '{
    "playerId": "test-player-1",
    "images": ["BASE64_FRONT_IMAGE", "BASE64_LEFT_IMAGE", "BASE64_RIGHT_IMAGE", "BASE64_BACK_IMAGE"],
    "angles": ["front", "left", "right", "back"]
  }'
```

### Тестирование игровой сессии

```bash
# Создаем игру
curl -X POST http://localhost:8080/api/game/start \
  -H "Content-Type: application/json" \
  -d '{
    "gameMode": "pvp",
    "maxPlayers": 2,
    "gameDuration": 15,
    "playerIds": ["test-player-1", "test-player-2"]
  }'

# Получаем статус игры (используйте gameId из предыдущего ответа)
curl "http://localhost:8080/api/game/status?gameId=GAME_ID_HERE"
```

## Разработка

### Локальная разработка Go backend

```bash
cd shoter-backend
go mod download
go run cmd/main.go
```

### Локальная разработка Python service

```bash
cd shoter-face_ifentifier
pip install -r requirements.txt
python app.py
```

### Переменные окружения

Основные переменные в `.env`:

- `DB_USER` - пользователь PostgreSQL
- `DB_PASSWORD` - пароль PostgreSQL  
- `DB_NAME` - имя базы данных
- `SERVER_PORT` - порт Go сервера (по умолчанию 8080)
- `JWT_SECRET` - секретный ключ для JWT токенов
- `SAVE_PROFILES_TO_DB` - сохранять ли профили в БД (true/false, по умолчанию true)

## Логи и отладка

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f server
docker-compose logs -f face-id
docker-compose logs -f postgres
```

### Подключение к базе данных

```bash
docker-compose exec postgres psql -U db_user -d mydb
```

## Безопасность

- Все профили игроков хранятся только в памяти Python сервиса
- Данные автоматически удаляются после окончания игровой сессии
- База данных используется только для логирования (опционально)

## Производительность

- Время распознавания лица: < 300ms
- Поддержка до 50 одновременных подключений
- Автоматическая очистка неактивных сессий

## Troubleshooting

### Проблемы с запуском

1. **Порты заняты**: Измените порты в `.env` файле
2. **Недостаточно памяти**: Увеличьте лимиты Docker
3. **Ошибки сборки**: Очистите Docker кэш: `docker system prune -a`

### Проблемы с распознаванием лиц

1. **"No face found"**: Убедитесь что на изображении четко видно лицо
2. **Низкая точность**: Используйте качественные фотографии с хорошим освещением
3. **Медленная работа**: Уменьшите размер изображений до 640x480

## Новая функциональность: Сохранение профилей в БД

### 🆕 Управление профилями в PostgreSQL

Добавлена возможность сохранения face embeddings в PostgreSQL базу данных с управлением через флаг `SAVE_PROFILES_TO_DB` в .env файле.

**Преимущества:**
- ✅ Постоянное хранение профилей игроков
- ✅ Возможность загрузки профилей между игровыми сессиями  
- ✅ Управление профилями через REST API
- ✅ Автоматическая очистка при удалении игрока
- ✅ Связь с основными таблицами через foreign keys

**Структура БД:**
```sql
CREATE TABLE face_embeddings (
    id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    player_id VARCHAR(50) NOT NULL,
    angle VARCHAR(20) NOT NULL,
    embedding JSONB NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (player_id) REFERENCES players(player_id) ON DELETE CASCADE,
    UNIQUE(player_id, angle)
);
```

**Тестирование:**
```bash
# Простой тест системы
./simple_test.sh

# Тест API для работы с БД
./test_db_api.sh
```

## Roadmap

- [x] Сохранение профилей в PostgreSQL
- [x] API для управления сохраненными профилями
- [ ] WebSocket для real-time обновлений
- [ ] Команды и турниры
- [ ] Статистика игроков
- [ ] Web интерфейс администратора
- [ ] Интеграция с GPS для локации
- [ ] Различные типы оружия